{% extends('FSBASTTAdminCrudBundle::layout.html.twig') %}

{% block main %}
    <h1>Liste des articles de presse</h1>

    <a class="btn btn-primary" href="{{ path('article_new') }}">Ajouter un article</a>
    <table class="records_list table table-striped table-bordered table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Sujet</th>
                <th>Date</th>
                <th>Fichier associé</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('article_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.subject }}</td>
                <td>{% if entity.date %}{{ entity.date|date('d/m/Y') }}{% endif%}</td>
                <td>{{ entity.file }}</td>
                <td>
                    <ul>
                        <li>
                            <a class="btn btn-primary" href="{{ path('article_show', { 'id': entity.id }) }}">Détail</a>
                        </li>
                        <li>
                            <a class="btn btn-primary" href="{{ path('article_edit', { 'id': entity.id }) }}">Editer</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a class="btn btn-primary" href="{{ path('article_new') }}">Ajouter un article</a>
{% endblock %}