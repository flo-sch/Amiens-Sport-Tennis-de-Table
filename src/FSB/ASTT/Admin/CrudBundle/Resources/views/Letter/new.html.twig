{% extends('FSBASTTAdminCrudBundle::layout.html.twig') %}

{% block main %}
    <h1>Création d'une lettre du club</h1>

    <form action="{{ path('letter_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <button class="btn btn-success" type="submit">Créer</button>
    </form>

    <ul class="record_actions">
        <li>
            <a class="btn btn-info" href="{{ path('letter') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}